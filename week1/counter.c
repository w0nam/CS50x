#include <stdio.h>
#include <cs50.h>

int main()
{
    int counter = 0;

    counter = counter + 1;
    //Add one to 'counter', not optimal synthax.
    counter += 1;
    //Add one to 'counter', better synthax.
    counter++;
    //Also add one to 'counter', best synthax.
}
